/**
 * Wave 7 (Capstone) — wire contract for GET /api/inventory/alerts.
 * Re-export from MaterialCatalogRepository for convenience.
 * FE is built against this exact shape — do NOT change field names.
 */
export type { LowStockAlertDTO } from '@domain/ports/MaterialCatalogRepository';
